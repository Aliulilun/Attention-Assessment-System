# 多模態注意力評估系統 — 架構說明文件

> 說明對象：對程式架構不熟悉的協作者、論文審閱者、或日後維護者。  
> 本文件依「**做什麼 → 怎麼做 → 為什麼這樣做**」三段式說明，並附常見問答。

---

## 目錄

1. [系統整體流程](#1-系統整體流程)
2. [入口程式：main.py vs hurry/main.py](#2-入口程式)
3. [聽覺大腦：語音模組](#3-聽覺大腦語音模組)
4. [視覺狀態機：牌子追蹤器](#4-視覺狀態機牌子追蹤器)
5. [動態模型管理員](#5-動態模型管理員)
6. [互動與指向判定引擎](#6-互動與指向判定引擎)
7. [視線估計管線](#7-視線估計管線)
8. [評分引擎](#8-評分引擎)
9. [推論與繪圖分離架構](#9-推論與繪圖分離架構)
10. [常見問答 Q&A](#10-常見問答-qa)

---

## 1. 系統整體流程

```
影片輸入
    │
    ▼
[語音模組] ──► 語音觸發時間窗 (T0 window) + 怪聲時間窗
    │
    ▼
┌──────────────── 幀迴圈（每一幀）────────────────────┐
│                                                    │
│  display_frame = frame.copy()                      │
│                                                    │
│  【推論區 — 全用乾淨 frame】                         │
│  ① OCR 牌子偵測 → 判斷 Stage 1-7                   │
│  ② YOLO 目標物偵測 → 取得目標框 yolo_boxes           │
│  ③ YOLO-Pose + MediaPipe → 指向射線命中              │
│  ④ 視線估計 → active_gaze（含防閃爍快取）             │
│                                                    │
│  【邏輯區 — 純計算】                                 │
│  ⑤ 射線碰撞判定 (Ray-AABB) → child_is_gazing_at    │
│  ⑥ TH 判定 → child_is_gazing_at_tester            │
│                                                    │
│  【繪圖區 — 全畫在 display_frame】                   │
│  ⑦ 畫框線、箭頭、骨架、UI 文字                       │
│                                                    │
│  【輸出區】                                         │
│  ⑧ scoring.update_frame() → 累計 TB/TH/Pointing    │
│  ⑨ out.write(display_frame) + imshow               │
└────────────────────────────────────────────────────┘
    │
    ▼
FFmpeg 音軌縫合 → 最終有聲影片
    │
    ▼
event_record.txt（含 T0/TB/TH 時間戳與分數）
```

---

## 2. 入口程式

### `main.py`（互動式單影片模式）

**做什麼：** 讓使用者輸入一支影片編號，從頭到尾分析，最後輸出有聲影片與事件紀錄。

**怎麼做：**
1. 啟動時用 `input()` 詢問影片編號（最多輸入兩次，失敗則自動選 video/ 下第一支 mp4）。
2. 初始化所有模組（語音、牌子、YOLO、InteractionEngine、GazePipeline）。
3. 進入幀迴圈，每幀依序做推論 → 邏輯 → 繪圖 → 評分 → 輸出。
4. 幀迴圈結束後呼叫 FFmpeg 縫合音軌，輸出 `output_result_final_1to14_5.mp4`。

**為什麼：** 單影片模式方便開發者即時看到 OpenCV 預覽視窗，隨時按 Q 中止，用於除錯與微調。

---

### `hurry/main.py`（批次模式）

**做什麼：** 依序自動處理 `video/` 資料夾下的所有 mp4，不需要手動操作。

**怎麼做：**
- 批次掃描所有影片，對每支呼叫 `process_single_video()` 函數。
- 在同一個 Python 行程內連續處理，省去重覆載入模型的冷啟動時間。
- 語音快取（`speech_cache.json`）存在時直接讀取，跳過 Whisper 分析（省 30-120 秒/影片）。

**為什麼：** 臨床評估時一次可能有數十支影片，批次模式讓研究人員隔夜跑完所有影片，隔天直接查看報告。

---

## 3. 聽覺大腦：語音模組

### `modules/speech.py`（觸發器前端）

**做什麼：**
- 決定哪些時間段是「關鍵字觸發時間窗」（施測者說出關鍵詞後開啟的 3 秒判定窗口）。
- 決定哪些時間段是「怪聲觸發時間窗」（noise.wav 範本命中的時間段，用於 Stage 7→8 跳階）。

**怎麼做：**
1. 優先讀取 `output/speech_cache.json`，若快取版本號吻合就直接解析，完全跳過 Whisper。
2. 無快取時啟動獨立子行程（subprocess）執行 `speech_engine.py`，子行程完成後再讀結果。
3. 從 JSON 讀出 `trigger_windows`（關鍵字窗口）與 `noise_events[].trigger_window`（怪聲窗口）。

**關鍵方法：**
- `is_in_window(t, windows)` — 判斷目前幀時間是否落在任一關鍵字窗口內。
- `is_in_noise_window(t)` — 判斷是否落在 noise.wav 命中窗口內，供 Stage 7→8 代償用。

**為什麼要用子行程：** Whisper large-v3 載入會佔用大量 VRAM，若和 YOLO 在同一行程就會 OOM（GPU 記憶體不足）。子行程結束後 VRAM 完全釋放，YOLO 才能正常跑。

---

### `modules/speech_engine.py`（Whisper 分析核心，獨立子行程執行）

**做什麼：**
- 從影片中提取音訊，用 OpenAI Whisper 進行語音辨識。
- 在逐字稿中搜尋關鍵字，建立 `trigger_windows`。
- 用 noise.wav 做音訊模板比對（Cosine Similarity ≥ 0.7），偵測怪聲時間段。

**為什麼設 0.7 門檻：** 0.7 是在「不漏判真實怪聲」和「不誤判背景雜音」之間實驗取得的平衡點；過高（0.85）會漏掉品質差的錄音，過低（0.58）會把電話鈴聲或咳嗽誤判為怪聲。

---

## 4. 視覺狀態機：牌子追蹤器

**檔案：** `modules/signboard.py`

### 做什麼
識別施測者舉起的「數字牌」（1-7），控制系統從 Stage 1 升階到 Stage 7。

### 怎麼做（四層防護設計）

| 層次 | 技術 | 目的 |
|------|------|------|
| ① 使用者框選 ROI | cv2.selectROI | 限定牌子可能出現的大範圍區域，避免背景誤判 |
| ② KCF 追蹤器 | cv2.TrackerKCF | OCR 每 2 幀才跑一次，中間幀由 KCF 維持綠框跟隨牌子位移 |
| ③ EasyOCR 識別 | easyocr + CLAHE | 在追蹤框內做 OCR，僅辨識 1-8 數字（allowlist='12345678'）|
| ④ 投票升階 | deque(maxlen=7) | 最近 7 次 OCR 結果中，同一數字需出現 ≥ 2 次才算確認升階（防單幀誤判） |

**Stage 7 特殊處理：** 由於數字 6 和 7 外形相似（6 和 7 在低解析度下易混淆），Stage 7 改用「模板比對（Template Matching）」，需連續 3 幀匹配分數 > OCR 讀出 6 的信心值才升階。

**KCF 防抖保護：**
- `KCF_JITTER_ZONE=3`：KCF 更新時若中心位移 < 3px 視為微抖動，不更新綠框（防止框在原地抖）
- `KCF_MAX_JUMP=40`：單幀位移 > 40px 視為誤跳（KCF 追丟），直接拒絕該更新

**遮擋偵測（直方圖比對）：**
- 牌子首次被 OCR 確認時，記錄它的外觀直方圖快照（`_appear_snapshot`）。
- 後續每幀比對直方圖相關係數，低於 `APPEAR_SIM_THRESHOLD=0.55` 時視為「手遮住牌子」，凍結綠框位置（不跟隨），避免手移動時框亂飄。
- 需連續 `LOW_SIM_STREAK_MAX=3` 幀低相似度才觸發凍結（防單幀誤判）。

**應急框機制：**
- 連續 25 幀（`NO_DETECT_FALLBACK`）完全偵測不到數字時，切換到「應急框」模式：把黃框重設回 ROI 中心 1.5 倍大小，強制做一次全域掃描。
- 連續 3 次（`FALLBACK_EXIT_STREAK`）OCR 成功才退出應急框，防止一幀偵測成功就立刻退出造成抖動。

**為什麼要 KCF 追蹤器：** EasyOCR 每幀跑很慢（約 100ms），若每幀都跑 OCR 系統會嚴重拖慢。KCF 追蹤只需幾毫秒，在兩次 OCR 之間維持綠框位置，讓系統看起來像即時追蹤。

---

## 5. 動態模型管理員

**檔案：** `modules/models_manager.py`

### 做什麼
依照當前 Stage 自動切換不同的 YOLO 偵測模型，辨識對應的目標物品。

### 模型對照表

| Stage | 模型檔案 | 信心度門檻 | 偵測目標 |
|-------|----------|-----------|----------|
| 1, 2 | `front_model.pt` | 0.70 | 桌面近端物品 |
| 3, 4 | `background_model.pt` | 0.75 | 遠方/背景物品 |
| 5 | `balloon_model.pt` | 0.75 | 神奇氣球 |
| 6 | `doll_model.pt` | 0.70 | 操偶 |
| 7 | `toy_model.pt` | 0.70 | 玩具箱 |
| 9, 10 | `tablet_model.pt` | 0.75 | 平板電腦 |
| 11, 12 | `front_model.pt` | **0.15** | 機器人指近端物品（低信心度，因實測約 0.22~0.33）|
| 13, 14 | `background_model.pt` | 0.75 | 機器人指遠端物品 |

Stage 9 起額外用 `robot_model.pt`（信心度 0.6）偵測機器人位置，回傳 `robot_boxes`，供 TH 判定「看回機器人」使用。

**面積過濾（Stage 1、2、6、7）：** 偵測框面積 > 畫面面積 40% 的框直接丟棄。目的是過濾掉誤把人體（施測者）判定為目標物的超大框，只留下合理尺寸的物品框。

**為什麼要動態切換：** 若把所有物品類別塞在一個模型裡，不同物品的特徵互相干擾，準確率大幅下降。獨立訓練各階段的專屬模型，讓每個模型只認識它負責的物品，準確率顯著提升。

**惰性載入（Lazy Loading）：** 模型只在第一次用到該 Stage 時才載入 VRAM，避免一次載入 8 個模型把 GPU 撐爆。

---

## 6. 互動與指向判定引擎

**檔案：** `modules/interaction.py`

### 做什麼
判斷兒童是否用食指「指向」目標物品（Pointing），並區分出施測者與兒童的手。

### 怎麼做

**Step 1 — 骨架偵測（YOLO11-Pose）**
- 偵測畫面中所有人的 17 個骨架關節點。
- 計算「手臂關節連動分數（Arm Link Score）」：手腕-肘-肩三點夾角，分數高者為手臂完整展開（施測者）、分數低者為兒童縮手姿勢。
- 據此區分施測者（分數高）與兒童（分數低）。

**Step 2 — 手部偵測（MediaPipe HandLandmarker，VIDEO 模式）**
- 偵測 21 個手部特徵點。
- 判斷食指是否伸直（PIP 角度 > 閾值）、其他手指是否彎曲（比較各指指尖-掌根距離）。

**Step 3 — 射線投射（Ray Casting）**
- 從食指指尖發出一條方向向量（指尖 → 食指第一關節的延伸線）。
- 用 Ray-AABB 演算法（軸對齊包圍盒射線碰撞）計算這條線是否穿過任一 YOLO 目標框。
- YOLO 框外擴 10% 作為命中容差（`expand_box(box, ratio=0.10)`），避免射線擦框緣一幀中一幀不中。

**為什麼要身份識別：** 施測者也會指向物品（示範動作），若不區分就會把施測者的指向誤計為兒童的反應，完全誤導評估結果。

**為什麼用 VIDEO 模式：** MediaPipe VIDEO 模式使用時間序列追蹤，比 LIVE_STREAM 更穩定。但 VIDEO 模式要求 timestamp 單調遞增，批次處理多支影片時需設定 `_ts_base` 偏移，確保不同影片的時間戳不重疊。

---

## 7. 視線估計管線

**模組：** `modules/gaze_estimation/`

### 做什麼
估計兒童眼睛看向的方向（Pitch/Yaw 角度 + 3D 視線向量），並判定是否看向目標物或施測者/機器人。

### 五段流程

```
1. 臉部偵測（YOLO-Head）
        ↓
2. MediaPipe FaceMesh（468 特徵點）
        ↓
3. 頭部姿態估計（PnP 求解 → Pitch/Yaw/Roll 歐拉角）
        ↓
4. 眼球注視方向修正（基於瞳孔位置）
        ↓
5. 輸出：gaze_vector（2D 投影向量）+ 信心值
```

### 視線射線碰撞判定（`is_gazing_at_box`）

```python
eye_center = (左眼 + 右眼) / 2         # 射線起點
direction  = gaze_vector                 # 射線方向
命中 = ray_intersects_box(eye_center, direction, target_box)
```

### 防閃爍快取機制（last_valid_gaze）

- 每幀做視線估計後，只有 `success=True` 才更新 `last_valid_gaze`。
- 估計失敗時，計數器 `gaze_fallback_counter += 1`，超過 5 幀才清空快取。
- 邏輯判定和繪圖都使用 `active_gaze = last_valid_gaze`，而非當幀原始結果。

**為什麼要防閃爍：** 兒童快速轉頭或被遮擋時，MediaPipe 可能有 1-3 幀失敗，若立即清空箭頭，視覺上會看到「箭頭閃爍消失」。快取機制讓箭頭平滑維持，直到真的失蹤超過 5 幀才消失。

---

## 8. 評分引擎

**檔案：** `modules/scoring_engine.py`

### 做什麼
依臨床評估協定，記錄每個任務的 T0、Pointing、TB（看向物品）、TH（看回施測者/機器人）的時間與次數，並計算總分。

### 時間軸自動建立（`_build_absolute_timeline`）

系統從語音快取中自動推算各 Stage 起始時間：

| Stage | 判斷依據 |
|-------|----------|
| 8 | noise.wav 命中時間，或語音事件關鍵字中含「怪聲」的最早時刻（**已移除「機器人」**，防止機器人登場語音被誤判為 Stage 8 起點）|
| 9 | 語音文字中含「畫/画」的第一個時間點（在 Stage 8 之後）|
| 10 | 語音文字中含「煙火/烟火/321/三二一/三/3」的第一個時間點（在 Stage 9 之後）|
| 11-14 | 語音文字同時含「小朋友」+「你看」的事件，取 Stage 10 之後、間隔 ≥ 2 秒的前 4 筆，依序對應 Stage 11-14 |

### T0 建立條件

- **Stage 1-4：** 語音出現「你看」時建立 T0（每 Stage 只建一筆）。
- **Stage 5-7：** YOLO 首次偵測到目標物時建立 T0。
- **Stage 8：** 怪聲/語音觸發時建立 T0，有效窗口為 T0 起 10 秒。
- **Stage 9-14：** 語音事件時間戳與絕對時間軸對齊（誤差 < 0.05 秒）時建立 T0。

---

## 9. 推論與繪圖分離架構

**為什麼要分離：** 系統同時運行 4 個以上的 AI 模型（OCR、YOLO 物偵、YOLO-Pose、MediaPipe、GazePipeline），若所有模型共用同一個 `frame`，先跑的模型畫的框線和文字會污染後面模型的輸入：

- OCR 讀到 YOLO 寫在畫面上的「Target」字樣，誤判為牌子數字。
- YOLO 的綠色框壓住機器人邊緣，框線破壞物體輪廓導致漏偵測。
- MediaPipe 偵測臉部特徵點時，若臉上疊了 Gaze 箭頭，特徵點位置會偏移，造成視線箭頭抖動或消失。

**解決方案：**
```python
display_frame = frame.copy()    # 每幀開頭：複製乾淨複本

# 推論：全部吃 frame（乾淨）
sign_tracker.detect_stage(frame)
model_manager.detect_objects(frame, stage)
gaze_pipeline.estimate(frame)

# 繪圖：全部畫在 display_frame
cv2.rectangle(display_frame, ...)
draw_gaze_with_face_box(display_frame, ...)

# 輸出：
out.write(display_frame)
cv2.imshow(..., display_frame)
```

---

## 10. 常見問答 Q&A

---

### Q1：次數統計（TB、TH、Pointing）怎麼計？怎樣才算「一次」？

#### Pointing（兒童指向物品）

**算一次的條件：**
1. `child_is_pointing_hit = True`（食指射線命中目標框）
2. **上升邊緣**：上一幀 `_prev_pointing = False`（也就是從「沒指」變成「指到」才計，持續指著不重複算）
3. **冷卻時間**：距離上次計次時間 > 0.8 秒（防止 YOLO 每幀偵測略有跳動造成一次指向被算成多次）

**舉例：**
```
幀 100: child_is_pointing_hit=True  → 首次命中，計 Pointing#1
幀 101: child_is_pointing_hit=True  → 同一次（上升邊緣未觸發），不計
幀 102: child_is_pointing_hit=False → 手放下
幀 130: child_is_pointing_hit=True  → 新的一次，但需距離#1 > 0.8s 才計 Pointing#2
```

---

#### TB（看向物品 Turning to Behavior）

**算一次的條件：**
1. `child_is_gazing_at = True`（視線射線命中 YOLO 目標框）
2. **上升邊緣**：上一幀 `_prev_tb = False`
3. **冷卻時間**：距離上次 TB 計次 > 1.5 秒
4. **🌟 交替計次**：`_last_counted_target ≠ "object"`（上一次不能也是看向物品，必須先看過人才能再計一次 TB）

**交替規則的意義：**
```
正確行為流程（每次都計）：
  物品 (TB#1) → 施測者 (TH#1) → 物品 (TB#2) → 施測者 (TH#2)

不計的情況（視線一直停在物品上）：
  物品 (TB#1) → 物品 (不計，未交替) → 物品 (不計)

不計的情況（視線移開又回到物品，中間沒看人）：
  物品 (TB#1) → 背景 → 物品 (不計，未先計 TH)
```

**為什麼要交替：** 臨床上「Joint Attention」的定義是「看物品 → 看人（分享注意力）→ 再看物品」的完整循環。兒童視線停留在物品上不管多久，都只算「一次看向物品」，必須真正轉頭看人（計了 TH）才能讓下次看物品算成第二次 TB。

---

#### TH（看回施測者/機器人 Turn to Head）

**算一次的條件：**
1. TB 已至少達成一次（有 TB 條件的 Stage 才需要）
2. `child_is_gazing_at_tester = True`（視線命中施測者區域 或 機器人框）
3. **上升邊緣** + **冷卻** 1.5 秒
4. **🌟 交替計次**：`_last_counted_target == "object"`（必須先計過 TB 才能計 TH）

**Stage 8 例外：** 怪聲任務沒有可以「看向」的目標物（TB 條件為 None），所以 TH 直接計次，不需要先有 TB。

---

### Q2：Stage 是怎麼切換的？

系統用三種機制決定當前 Stage，三者有優先順序：

1. **視覺判斷（OCR，Stage 1-7）：** 當 `current_stage < 8` 時，每幀讀取數字牌，若偵測到有效數字且投票通過就切換。
2. **絕對時間軸推進（Stage 8-14）：** `ScoringEngine._build_absolute_timeline()` 在初始化時從語音快取中推算各 Stage 起始秒數，主迴圈每幀比對目前時間，到了就強制推進。
3. **聽覺代償（Stage 7→8）：** 若 Stage 7 的牌子未被 OCR 偵測到，當 `speech.is_in_noise_window(t) == True` 時強制跳到 Stage 8。

```
Stage 1-7: 以 OCR 牌子為主，時間軸為輔
Stage 8-14: 以時間軸為主（因為不再有牌子）
Stage 7→8: 聽覺代償兜底（怪聲 noise.wav 命中）
```

---

### Q3：怪聲是怎麼偵測的？noise.wav 做什麼？

**noise.wav：** 是施測者在 Stage 7 用手機播放的特定怪聲音效的範本音檔（事先錄製好）。

**偵測流程：**
1. `speech_engine.py` 把影片音訊切成重疊滑動窗口（每段約 1-2 秒）。
2. 對每個時間窗口提取 MFCC 特徵，和 noise.wav 的 MFCC 做 Cosine Similarity。
3. 相似度 ≥ 0.7 的窗口標記為「怪聲命中」，紀錄為 `noise_events[].trigger_window`。
4. 主迴圈呼叫 `speech.is_in_noise_window(t)`，若落在命中窗口內就觸發 Stage 7→8 跳階。

**為什麼不用 Whisper 關鍵字偵測怪聲：** 怪聲是電子音效，沒有語音內容，Whisper 會把它辨識成「[聲音]」「嗶」「逼」等「聲音符號」，這些符號不穩定且容易誤觸發。noise.wav 模板比對直接比音訊特徵，比關鍵字比對更精準。

---

### Q4：視線判定「看向目標物」的原理是什麼？

```
1. 計算眼睛中心座標：
   eye_center = (left_eye + right_eye) / 2

2. 取得視線方向向量：
   direction = gaze_vector  （由 GazePipeline 的頭部姿態 + 瞳孔位置修正得到）

3. 射線碰撞測試（Ray-AABB）：
   從 eye_center 往 direction 方向發射一條無限長的射線
   若射線穿過任一 YOLO 目標框（外擴 10%） → child_is_gazing_at = True
```

**Pitch/Yaw 角度限制（看施測者）：** 視線看向施測者需同時滿足：
- `pitch_deg > -5`（不能低頭太多）
- `yaw_deg > 10`（需往施測者方向偏轉）

---

### Q5：每個 Stage 的分數怎麼算？什麼是反應等級？

每個 Stage 結束後，`stage_scoring.py` 依據 **ja判定公式.xlsx** 的評分規則計算：

| 事件 | 得分 |
|------|------|
| TB（看向物品）有發生 | +1 分 |
| TH（看回施測者/機器人）有發生 | +2 分 |
| Pointing（食指指向物品）有發生 | +1 分 |
| 合計 | 0 ~ 4 分 |

**反應等級對照：**

| 總分 | 等級（一般關卡） | 等級（遠物關卡 Far）|
|------|-----------------|---------------------|
| 0 | F（失敗）| F |
| 1 | LR（低度反應）| HR（高度反應）|
| 2 | HI（高度主動）| HI |
| 3 | LI（低度主動）| LI |
| 4 | HI（高度主動）| HI |

「遠物關卡」= Stage Label 含 "Far"（即真人指遠物、機器人指遠物，Stage 3、4、13、14）。

這個等級直接附加在 event_record.txt 的 Stage 標題行後面，例如：
```
Stage 3 -- 真人指遠物(Pointing - Far) -- 反應等級 HR(1分)
```

`total_score`（記錄有幾個 Stage 有任何反應）和 `stage_gazing_counts`（各 Stage 注視次數）是獨立統計，不影響反應等級。

---

### Q6：`hurry/main.py` 和 `main.py` 有什麼不同？

| 項目 | main.py | hurry/main.py |
|------|---------|----------------|
| 模式 | 單影片，互動式 | 批次，自動掃描 video/ |
| 影片選擇 | input() 手動輸入 | glob 自動掃描 |
| YOLO_SKIP / GAZE_SKIP | 逐幀（1） | 逐幀（1） |
| OCR_SKIP | 不獨立設定 | 5（每 5 幀執行一次 OCR）|
| 預覽視窗 | 每幀顯示 | 每幀顯示（可用 SHOW_PREVIEW=False 關閉）|
| Stage 支援 | Stage 1-14 | Stage 1-10 |
| 輸出目錄 | output/ | output/{影片名稱}/ |
| 怪聲觸發詞 | 由 speech.py 負責 | WEIRD_SOUND_TRIGGERS=["機器人"]（純口說詞；怪聲本身改由 noise.wav 模板比對）|

---

### Q7：計時器（_t_ocr、_t_yolo 等）是做什麼的？

是效能診斷工具，每 50 幀印一次各段平均耗時（毫秒/幀）：

```
⏱️ [Frame 50] 各段平均耗時（ms/幀）
   OCR=45.2  YOLO=23.1  Interact=18.5  Gaze=67.3  Other=5.1  Total=159.2ms
```

若某段耗時異常高，可針對該段做優化（如調整跳幀、降低解析度、換用更小的模型）。

---

### Q8：event_record.txt 裡的數據怎麼讀？

```
01. Stage 6 -- 看偶寫字 (Puppet Writing)
    T0       = 45.32s
    Pointing = 47.15s  (+1.83s from T0) x2
    TB       = 46.01s  (+0.69s from T0) x3
    TH       = 47.88s  (+2.56s from T0 / +1.87s from TB) x2
    Sequence = T0 -> Pointing OK -> TB OK -> TH OK
```

- **T0** = 任務開始時刻（語音關鍵字 or YOLO 首偵測 or 怪聲命中）
- **Pointing** = 兒童首次用食指指向目標的時刻，`x2` 表示此任務共發生 2 次
- **TB** = 兒童首次看向目標物的時刻，RT（Reaction Time）= TB − T0 = 0.69s，`x3` 表示共 3 次
- **TH** = 兒童首次看回施測者的時刻，從 TB 算起再過 1.87s
- **Sequence** = 是否完成完整 T0 → Pointing → TB → TH 序列

---

*文件最後更新：由系統自動生成，對應目前程式碼狀態。*
