# 兒童注意力監測與量化分析平台：技術方法與程式架構說明

> 本文件整理自專案 README 與系統架構說明文件，供研究合作者撰寫論文之**研究方法（Methods）**與**研究結果（Results）**段落參考使用。
> 專案倉庫：`https://github.com/Aliulilun/Attention-Assessment-System`（branch: `integrate_ver1`）

---

## 目錄

1. [系統總覽](#1-系統總覽)
2. [各階段技術模組與模型說明](#2-各階段技術模組與模型說明)
   - 2.1 [語音辨識模組](#21-語音辨識模組)
   - 2.2 [牌子（數字看板）辨識模組](#22-牌子數字看板辨識模組)
   - 2.3 [視線估計模組](#23-視線估計模組)
   - 2.4 [指向判斷模組](#24-指向判斷模組)
   - 2.5 [YOLO 多模型管理（Side Project 客製化偵測模型）](#25-yolo-多模型管理side-project-客製化偵測模型)
   - 2.6 [計分引擎與聯合注意力等級判定](#26-計分引擎與聯合注意力等級判定)
   - 2.7 [深度學習模型應用總覽（YOLO、ResNet 原理說明）](#27-深度學習模型應用總覽yoloresnet-原理說明)
3. [程式碼檔案架構整理](#3-程式碼檔案架構整理)
4. [組員分工](#4-組員分工)

---

## 1. 系統總覽

本平台整合**電腦視覺、語音辨識與深度學習**，自動化分析受測兒童在結構化測驗情境（真人指物、社會情境事件、機器人互動共 14 個 Stage）中的聯合注意力（Joint Attention, JA）行為，並依三項核心行為指標——**Pointing（指向）**、**TB（Turning Behavior，注視目標物）**、**TH（Turn-back to Human/Robot，社會性回視）**——自動計算各題反應等級，協助臨床工作者進行早期篩查與量化評估。

系統採**推論與繪圖分離架構**：每幀影像先以乾淨畫面分別送入 OCR、YOLO、YOLO-Pose、MediaPipe、視線估計等多個模型進行推論，避免模型間互相繪製的標註（框線、文字）污染彼此輸入，再統一將結果繪製於顯示畫面，確保多模型並行運作時的偵測準確度。

---

## 2. 各階段技術模組與模型說明

### 2.1 語音辨識模組

**對應檔案：** `modules/speech.py`（觸發器前端）、`modules/speech_engine.py`（推論核心，獨立子行程執行）

**使用模型：** OpenAI Whisper **large-v3**（本地端離線推理）

**為何選用此模型：**
- Whisper large-v3 為當前開源語音辨識模型中，在中文語境下具備高準確率與強健性的選項，且可完全離線運行，避免將含個資的兒童受測影音上傳雲端所產生的隱私疑慮，符合臨床研究資料保護需求。
- 系統需要辨識施測過程中的特定關鍵詞（如「你看」「小朋友你看」「畫」「321/煙火」等）作為各 Stage 的時間起點（T0），Whisper 在長語音逐字稿轉錄與時間戳對齊上的表現，可支援後續以「絕對時間軸」方式推算 Stage 8～14 的起訖時間。

**技術設計要點：**
- **關鍵字觸發窗機制**：以 `trigger_window` 標記關鍵詞出現前後約 3 秒的語音影響窗，作為後續判定 T0 的依據。
- **子行程隔離設計**：Whisper 推論以獨立 subprocess 執行，因其載入時佔用大量 VRAM，若與 YOLO 系列模型共用同一行程易導致 GPU 記憶體溢出（OOM）；子行程結束後 VRAM 完全釋放，YOLO 才能正常運作。
- **怪聲偵測改用音訊模板比對而非語音關鍵字**：Stage 8 的手機怪聲為電子音效、無語音內容，若用 Whisper 辨識容易被誤轉錄為不穩定的「聲音符號」（如「嗶」「逼」），因此改採 `noise.wav` 範本音檔，將影片音訊切成滑動窗口後取 MFCC 特徵，與範本做 **Cosine Similarity** 比對，相似度 ≥ 0.7 視為命中。此門檻值為實驗權衡結果：過高（0.85）會漏判品質較差的錄音、過低（0.58）則容易將電話鈴聲、咳嗽等背景雜音誤判為怪聲。
- **語音快取機制**：首次執行後將 Whisper 辨識結果與觸發事件寫入 `speech_cache.json`，後續重跑同一影片可直接讀取快取，避免重複推理，大幅縮短批次處理耗時。

---

### 2.2 牌子（數字看板）辨識模組

**對應檔案：** `modules/signboard.py`

**使用模型：** EasyOCR（字元白名單限定為數字 1～8）

**為何選用此模型：** EasyOCR 為開源、支援多語言的輕量級 OCR 工具，透過限制辨識字元集（allowlist）大幅提升在特定應用場景（施測者手持數字牌）下的辨識穩定度與速度，適合即時串流影像處理。此模組僅負責 Stage 1～7 的視覺判斷，Stage 8 以上改採語音絕對時間軸推進，因後續題目不再出現數字牌。

**四層防護設計（提升即時追蹤穩定性）：**

| 層次 | 技術 | 目的 |
|------|------|------|
| ① 使用者框選 ROI | `cv2.selectROI` | 限定牌子可能出現的大範圍區域，避免背景誤判 |
| ② KCF 追蹤器 | `cv2.TrackerKCF` | OCR 僅每 2 幀執行一次，中間幀由 KCF 維持追蹤框跟隨牌子位移，兼顧速度與即時性 |
| ③ EasyOCR + CLAHE | 直方圖等化強化對比度後再辨識 | 於追蹤框內執行 OCR，僅辨識目標數字 |
| ④ 投票升階機制 | `deque(maxlen=7)` | 最近 7 次 OCR 結果中，同一數字須出現 ≥ 2 次才確認升階，避免單幀誤判造成 Stage 錯誤跳動 |

**其餘穩定性設計：**
- **Stage 7 特殊處理**：因數字 6、7 在低解析度下外形相似，改用**模板比對（Template Matching）**，須連續 3 幀匹配分數高於 OCR 讀出 6 的信心值才升階。
- **KCF 防抖保護**：位移 < 3px 視為微抖動不更新；單幀位移 > 40px 視為追丟直接拒絕更新。
- **遮擋偵測（直方圖比對）**：牌子首次確認時記錄外觀直方圖快照，後續每幀比對相關係數，低於門檻（0.55）且連續 3 幀時判定為手部遮擋，凍結追蹤框位置。
- **應急框機制**：連續 25 幀完全偵測不到數字時，自動放大搜索範圍重新掃描；需連續 3 次成功才退出應急模式，避免偶發成功造成畫面抖動。

**為何需要 KCF 追蹤器：** EasyOCR 單幀推論耗時約 100ms，若逐幀執行會嚴重拖慢系統即時性；KCF 追蹤僅需數毫秒，可在兩次 OCR 之間維持追蹤框的視覺連續性。

---

### 2.3 視線估計模組

**對應檔案：** `modules/gaze_estimation/`（`gaze_pipeline.py` 及 stage1～stage5 子模組）

**核心模型：ETH-XGaze**（ECCV 2020，Zhang et al.）搭配 ResNet-50 主幹網路

**為何選用 ETH-XGaze：** 該模型以大規模、涵蓋極端頭部姿態與視線方向的資料集訓練而成，相較一般僅涵蓋正面或小角度轉頭的視線估計模型，更適合兒童在施測過程中頻繁轉頭觀察不同物品、施測者與機器人的真實場景需求。

**五階段視線估計管線：**

| 階段 | 內容 | 使用技術 |
|------|------|----------|
| Stage 1：人臉偵測 | 快速定位人臉候選框 | YOLO nano |
| | 提取 468 個 3D 面部特徵點 | MediaPipe Face Landmarker（Tasks API） |
| Stage 2：頭部姿態估計 | 求解 3D 旋轉矩陣與平移向量 | OpenCV `solvePnP`（ITERATIVE），搭配 ETH-XGaze 3D 頭部模型 |
| Stage 3：影像正規化 | 依頭部姿態將眼部區域正規化至標準視角，輸出 224×224 眼部影像 | 幾何正規化 |
| Stage 4：視線推理網路 | 輸入正規化眼部影像，輸出 (pitch, yaw) 視線角度 | ETH-XGaze ResNet-50（`epoch_24_ckpt.pth.tar`），並套用 Pitch 偏移校正（預設 -12.5°，可依實驗環境調整） |
| Stage 5：視線向量轉換 | 將角度轉換為 3D 視線方向向量，並投影至 2D 畫面座標 | 供後續 Ray-AABB 碰撞判定使用 |

**時序有限狀態機（GazeFSMManager）：** 當面部特徵點因兒童極端轉頭而消失時，系統進入 `EXTREME_TURNING` 狀態，利用前幀頭部位置與 YOLO 頭部框進行盲追蹤，維持視線方向的連續估計，避免因短暫丟幀造成注視事件遺漏。

**防閃爍快取機制：** 每幀僅在估計成功（`success=True`）時更新 `last_valid_gaze`；估計失敗時計數累加，超過 5 幀才清空快取，邏輯判定與繪圖皆使用此快取值而非當幀原始結果，避免因兒童快速轉頭或遮擋造成的 1～3 幀偵測失敗，導致視線箭頭在畫面上閃爍消失。

**視線碰撞判定（TB / TH 判定基礎）：** 以雙眼中心為射線起點、視線向量為方向，透過 **Ray-AABB（軸對齊包圍盒射線碰撞）演算法**判定視線是否穿過目標物包圍框（TB）或施測者/機器人所在區域（TH，須同時滿足 Pitch > -5°、Yaw > 10° 之角度限制）；同一次注視事件設有 1.5 秒冷卻時間，防止 YOLO 掉幀造成的重複計數。

---

### 2.4 指向判斷模組

**對應檔案：** `modules/interaction.py`

判斷兒童是否用食指指向目標物品，並需先區分「施測者」與「兒童」，避免將施測者的示範性指向誤計為兒童反應。

**技術流程：**

1. **骨架偵測（YOLOv11-Pose，`yolo11n-pose.pt`）**：偵測畫面中所有人的 17 個骨架關節點，並計算「手臂關節連動分數（Arm Link Score）」——以手腕-肘-肩三點夾角判斷手臂伸展程度，分數高者判定為施測者（手臂完整展開示範）、分數低者判定為兒童（縮手姿勢），並以畫面左側 35% 區域作為施測者區邊界輔助判斷。
2. **手部偵測（MediaPipe Hand Landmarker，Tasks API，VIDEO 模式）**：提取 21 個手部特徵點，判斷食指是否伸直（PIP 角度門檻）、其餘手指是否彎曲（比較指尖與掌根距離），過濾握拳或手掌邊緣造成的誤觸發。
3. **射線投射與碰撞判定（Ray-AABB）**：自食指指尖延伸出方向向量，判定該射線是否穿過任一 YOLO 目標框；目標框額外外擴 10% 作為命中容差，避免射線擦過框緣導致的偵測不穩定（一幀中一幀不中）。
4. **SMA 平滑處理**：以簡單移動平均（window=5）平滑骨架關鍵點座標，降低 YOLO 偵測掉幀造成的抖動。

**為何選用 YOLOv11-Pose + MediaPipe Hands 的組合而非單一模型：** 骨架偵測負責大範圍的身分辨識（施測者 vs. 兒童），手部關鍵點偵測則負責精細的手指伸直判斷，兩者分工互補，兼顧判斷的粗粒度（誰在動作）與細粒度（動作是否為有效指向）需求。

**為何採用 MediaPipe VIDEO 模式：** VIDEO 模式運用時間序列追蹤，較 LIVE_STREAM 模式更穩定；但因其要求時間戳單調遞增，批次處理多支影片時需額外設定時間戳偏移量，避免不同影片間的時間戳重疊造成錯誤。

---

### 2.5 YOLO 多模型管理（Side Project 客製化偵測模型）

**對應檔案：** `modules/models_manager.py`

系統依測驗題目（Stage）動態切換不同的**客製化訓練 YOLO 模型**，而非使用單一通用模型辨識所有物品類別：

| Stage | 模型檔案 | 偵測目標 |
|-------|---------|----------|
| 1～2 | `front_model.pt` | 近端目標物（桌面近端物品） |
| 3～4 | `background_model.pt` | 遠端目標物（背景物品） |
| 5 | `balloon_model.pt` | 神奇氣球 |
| 6 | `doll_model.pt` | 操偶（看偶寫字） |
| 7 | `toy_model.pt` | 玩具驚喜袋 |
| 9～10 | `robot_point_model.pt`（或 `tablet_model.pt`） | 平板/機器人 |
| 11～14 | `robot_point_model.pt` + 固定 ROI | 機器人指向之近/遠端物品 |

**為何採用多模型客製化訓練而非單一通用模型：** 若將所有物品類別合併於單一模型訓練，不同物品之視覺特徵（如氣球、玩偶、玩具箱）彼此干擾，會顯著降低整體辨識準確率；改採各 Stage 獨立訓練專屬模型後，每個模型僅需學習其負責的特定物品類別，準確率明顯提升。此設計也是團隊訓練 YOLO 模型的主要 side project 內容之一。

**其餘工程設計：**
- **惰性載入（Lazy Loading）**：模型僅在對應 Stage 首次被觸發時才載入 VRAM，避免一次性載入全部模型導致 GPU 顯存溢出。
- **硬體加速**：自動偵測 CUDA 可用性，支援 FP16 半精度推理以提升速度。
- **信心度門檻依 Stage 客製化調整**：例如 Stage 11、12（機器人指近端物品）實測信心度偏低（約 0.22～0.33），因此門檻特別下修至 0.15，避免因機器人手臂遮擋物品導致的低信心值造成漏偵測；其餘 Stage 則採 0.70～0.75 之較高門檻以降低誤判。
- **面積過濾**：於部分 Stage（如 1、2、6、7）中，偵測框面積超過畫面 40% 者直接丟棄，避免誤將施測者身體判定為目標物。
- **固定 ROI 鎖定機制（Stage 11～12）**：近端物品位置固定，系統在 ROI 內首次偵測到物品後即永久鎖定該框位置，後續幀不再重複執行 YOLO 推論，藉此節省運算資源。

**輔助頭部偵測模型引用致謝：** 視線估計 Stage 1 的頭部初步定位另參考開源專案 **YOLOv8_head_detector**（作者 Abcfsa）之預訓練權重，顯著提升頭部偵測階段的強健性與效率。

---

### 2.6 計分引擎與聯合注意力等級判定

**對應檔案：** `modules/scoring_engine.py`（計分核心）、`modules/stage_scoring.py`（等級判定）

**計分邏輯：** 每個 Stage 依 TB、TH、Pointing 三項指標是否達成計分（TB = 1 分、TH = 2 分、Pointing = 1 分，合計 0～4 分），並依分數與題目類型（近端/遠端）對應至五種聯合注意力反應等級：**F（失敗）、LR/HR（低度/高度反應）、LI（低度主動）、HI（高度主動）**。

**核心工程機制：**
- **絕對時間軸建立**：初始化時從語音快取自動推算 Stage 8～14 各題起始時間（例如 Stage 9 以語音「畫」關鍵字首次出現時間為 T0、Stage 11～14 依序取 4 次「小朋友你看」事件對應）。
- **上升邊緣偵測（Rising Edge Detection）**：TB/TH/Pointing 僅在狀態由「未達成」轉為「達成」的第一幀觸發計次，避免持續維持同一狀態時被重複計數。
- **冷卻時間（Cooldown）**：TB/TH 間隔須 ≥ 1.5 秒、Pointing 間隔須 ≥ 0.8 秒，防止 YOLO 或視線估計偶發性掉幀造成的重複判定。
- **交替計次規則（臨床定義還原）**：TB 與 TH 須交替發生方能各自累計次數——即兒童須完成「看物品 → 看人（分享注意力）→ 再看物品」的完整循環，視線持續停留在物品上不論多久，僅算一次 TB，唯有中間確實計過一次 TH，才能讓下一次看物品計為第二次 TB。此設計目的在於忠實還原臨床上「聯合注意力」須包含社會性分享回視的定義，而非僅偵測單純的注視停留時間。

---

### 2.7 深度學習模型應用總覽（YOLO、ResNet 原理說明）

前面各小節已分別介紹各模組使用的模型，本節將系統中**所有深度學習模型的應用位置**集中整理，並簡要說明 YOLO 與 ResNet 兩大核心架構的運作原理，供論文中「深度學習方法」段落統一引用。

#### 2.7.1 系統中深度學習模型應用位置一覽

| 模型類型 | 具體模型 | 應用位置 | 任務類型 |
|---------|---------|----------|----------|
| **YOLO（物件偵測，Detection）** | `front_model.pt` / `background_model.pt` / `balloon_model.pt` / `doll_model.pt` / `toy_model.pt` / `robot_point_model.pt` | 各 Stage 目標物、機器人偵測（`models_manager.py`） | 物件偵測（框出目標物位置＋類別） |
| **YOLO（人臉偵測）** | `nano.pt`（YOLO nano） | 視線估計管線 Stage 1：人臉候選框初步定位 | 物件偵測（人臉） |
| **YOLO（人體骨架偵測，Pose Estimation）** | `yolo11n-pose.pt`（YOLOv11-Pose） | 指向判斷模組：偵測施測者/兒童 17 個骨架關節點 | 關鍵點偵測（Keypoint Detection） |
| **ResNet（影像特徵萃取＋迴歸）** | ResNet-50（`epoch_24_ckpt.pth.tar`，ETH-XGaze 架構) | 視線估計管線 Stage 4：由正規化眼部影像輸出 (pitch, yaw) 視線角度 | 迴歸（Regression，輸出連續數值而非類別框） |
| （輔助對照）Transformer | OpenAI Whisper large-v3 | 語音辨識模組 | 序列到序列（語音→文字） |
| （輔助對照）CNN-based Landmark 模型 | MediaPipe Face Landmarker / Hand Landmarker | 視線估計、指向判斷 | 關鍵點迴歸（468 個臉部特徵點／21 個手部特徵點） |

> 上表後兩項（Whisper、MediaPipe）為第三方預訓練深度學習模型，系統直接調用其推論結果而未重新訓練；YOLO 系列與 ResNet 則為系統核心自行訓練或微調（fine-tune）之客製化模型（除 YOLO nano、YOLOv11-Pose 為官方預訓練權重直接使用外），是本專案在方法學上的主要貢獻。

---

#### 2.7.2 YOLO（You Only Look Once）運作原理

YOLO 屬於**單階段（One-Stage）物件偵測模型**，其核心概念是將物件偵測問題轉化為單一回歸問題：

1. **端到端單次推論**：傳統兩階段偵測器（如 R-CNN 系列）須先產生候選區域（Region Proposal）再逐一分類，運算量大、速度慢；YOLO 則將整張影像一次送入卷積神經網路（CNN），單次前向傳播（forward pass）即同時輸出所有候選框的**位置座標（bounding box）**、**目標存在信心度（objectness confidence）**與**類別機率（class probability）**，因此速度遠快於兩階段方法，適合本系統逐幀即時分析影片的需求。
2. **骨幹—頸部—偵測頭架構（Backbone–Neck–Head）**：
   - **Backbone**：以卷積層堆疊萃取影像的多尺度特徵（由淺層的邊緣、紋理到深層的語意特徵）。
   - **Neck**：融合不同尺度的特徵圖（如 FPN/PAN 結構），使模型同時具備偵測大物件與小物件的能力。
   - **Head**：在特徵圖上輸出每個網格（grid cell）或每個特徵點對應的框座標、信心度與類別，本系統使用的 YOLOv8/v11 系列採用 **Anchor-free** 設計，直接預測目標中心點與寬高，省去傳統 anchor box 的手動設計與調校。
3. **信心度門檻與非極大值抑制（NMS）**：模型會針對同一目標產生多個重疊候選框，系統再以信心度門檻（如本系統各 Stage 依實測情況設定 0.15～0.75 不等）過濾低信心結果，並以 NMS 去除重複框，僅保留最佳偵測結果。

**在本系統中的三種應用形式：**
- **物件偵測**（各 Stage 目標物、機器人）：輸出目標物的邊界框，供後續 Ray-AABB 射線碰撞判定（TB／Pointing／TH）使用。
- **人臉偵測**（YOLO nano）：作為視線估計管線的第一階段，快速圈出人臉概略位置，縮小 MediaPipe 後續精細特徵點偵測的搜索範圍，提升整體管線效率。
- **人體姿態／關鍵點偵測**（YOLOv11-Pose）：偵測架構與物件偵測相同（單階段、CNN 特徵萃取），差異在於輸出頭由「框座標」改為「17 個關節點座標」，用於指向判斷模組中區分施測者與兒童的手臂姿態。

**為何各 Stage 分別訓練客製化模型而非單一通用模型：** 已於 2.5 節說明——不同物品類別特徵混合訓練會互相干擾，獨立訓練可提升各自準確率。

---

#### 2.7.3 ResNet（Residual Network）運作原理

ResNet 是視線估計模組中 ETH-XGaze 視線推理網路的骨幹架構（Stage 4），其核心概念為**殘差學習（Residual Learning）**：

1. **解決深層網路退化問題**：一般認為卷積神經網路層數越深，理論上能學到的特徵越豐富、準確率應越高；但實務上單純堆疊卷積層達到一定深度後，會出現**梯度消失／退化問題（Degradation Problem）**——深層網路的訓練誤差反而高於較淺層網路，並非過擬合所致，而是深層網路難以學習到「恆等映射（Identity Mapping）」。
2. **殘差連接（Skip Connection / Shortcut）**：ResNet 在每個殘差區塊（Residual Block）中，讓輸入 `x` 直接跳接加到該區塊卷積運算後的輸出 `F(x)` 上，即輸出為 `F(x) + x`，而非單純的 `F(x)`。如此一來，網路只需學習「殘差」（輸入與目標輸出之間的差異）而非完整映射，大幅降低深層網路的訓練難度，使得訓練 50 層以上的深層網路（本系統採用 **ResNet-50**）成為可行，讓模型能萃取更深層、更具鑑別力的影像特徵。
3. **在視線估計中的角色（迴歸任務）**：與 YOLO 的「偵測＋分類」任務不同，ResNet-50 在此扮演的是**迴歸模型（Regression Model）**的角色——輸入為經正規化的 224×224 眼部影像，網路萃取深層特徵後，最終輸出兩個連續數值（pitch、yaw 視線角度），而非離散的類別或框座標。這也是為何視線估計需要先經過「頭部姿態估計＋影像正規化」（Stage 2、3）將眼部影像對齊至標準視角，再交由 ResNet 進行角度回歸，因為 ResNet 對輸入影像的視角一致性較為敏感，正規化步驟能大幅提升角度預測的準確度與穩定性。

**為何視線估計選用 ResNet 架構而非直接用 YOLO 類型模型：** YOLO 的偵測頭設計適合輸出「離散的框＋類別」，但視線角度是連續數值的迴歸問題，需要模型有能力萃取細膩的瞳孔／眼球外觀特徵並映射至連續角度空間；ResNet 深層殘差結構在影像分類與迴歸任務上已被廣泛驗證具備優異的特徵萃取能力，也是 ETH-XGaze 原始論文採用的骨幹架構，故本系統延用此設計以確保與原始預訓練權重相容。

---

## 3. 程式碼檔案架構整理

> 依系統架構說明文件整理，依「做什麼 → 怎麼做 → 為什麼這樣做」原則說明各檔案功能，供論文方法段落或系統實作附錄引用。

### 3.1 整體資料流程

```
影片輸入
  └─► 語音模組（先行離線分析）→ 語音觸發時間窗（T0 window）+ 怪聲時間窗
        └─► 幀迴圈（逐幀處理）
              ├─ 推論區（皆讀取乾淨 frame，避免互相污染）
              │   ① OCR 牌子偵測 → 判斷 Stage 1-7
              │   ② YOLO 目標物偵測 → 取得目標框
              │   ③ YOLO-Pose + MediaPipe → 指向射線命中判定
              │   └─ ④ 視線估計 → 有效視線角度（含防閃爍快取）
              ├─ 邏輯區（純計算）
              │   ⑤ 射線碰撞判定（Ray-AABB）→ TB 判定
              │   └─ ⑥ TH 判定
              ├─ 繪圖區（統一畫在顯示畫面複本）
              │   ⑦ 框線、箭頭、骨架、UI 文字
              └─ 輸出區
                  ⑧ 計分引擎更新（累計 TB/TH/Pointing）
                  └─ ⑨ 輸出影片幀 + 即時預覽
  └─► FFmpeg 音軌縫合 → 最終有聲標注影片
  └─► event_record.txt（完整事件紀錄與計分報告）
```

### 3.2 入口程式

| 檔案 | 做什麼 | 怎麼做 | 為什麼 |
|------|--------|--------|--------|
| `main.py` | 互動式單影片分析模式 | 使用者輸入影片編號 → 初始化所有模組（語音、牌子、YOLO、互動引擎、視線管線）→ 逐幀推論/邏輯/繪圖/評分/輸出 → 結束後以 FFmpeg 縫合音軌 | 提供即時 OpenCV 預覽視窗，方便開發者除錯與參數微調 |
| `hurry/main.py` | 批次自動處理模式 | 自動掃描 `video/` 資料夾下所有 mp4，對每支呼叫 `process_single_video()`，同行程內連續處理省去模型重複載入時間；若語音快取已存在則跳過 Whisper 分析 | 臨床研究常需一次處理數十支影片，批次模式可支援研究人員隔夜跑完全部影片、隔天直接檢視報告 |

### 3.3 核心功能模組（`modules/`）

| 檔案 | 角色 | 關鍵設計 |
|------|------|----------|
| `speech.py` | 語音觸發器前端 | 優先讀取 `speech_cache.json`，無快取時以子行程啟動 `speech_engine.py`；提供 `is_in_window()`、`is_in_noise_window()` 供主迴圈查詢目前是否落在關鍵字/怪聲觸發窗內 |
| `speech_engine.py` | Whisper 推論核心（獨立子行程） | 音訊擷取 → Whisper 語音辨識 → 關鍵字比對建立 `trigger_windows`；並以 MFCC + Cosine Similarity 比對 `noise.wav` 偵測怪聲窗口 |
| `signboard.py` | 牌子（數字看板）視覺狀態機 | ROI 框選 + KCF 追蹤 + EasyOCR 辨識 + 投票升階四層防護，控制 Stage 1→7 之升階邏輯 |
| `models_manager.py` | YOLO 多模型動態管理 | 依當前 Stage 惰性載入對應客製化模型、動態調整信心度門檻、面積過濾、CUDA/FP16 加速 |
| `interaction.py` | 指向互動判定引擎 | YOLOv11-Pose 骨架偵測（區分施測者/兒童）+ MediaPipe Hands 手指伸直判定 + Ray-AABB 射線碰撞，輸出 Pointing 命中結果 |
| `scoring_engine.py` | 計分核心 | 初始化時建立 Stage 8～14 絕對時間軸；`update_frame()` 逐幀更新 T0/TB/TH/Pointing 觸發紀錄；結束後輸出 `event_record.txt` |
| `stage_scoring.py` | 聯合注意力等級判定 | 依 TB/TH/Pointing 得分與題目類型（近端/遠端）對應 F/LR/HR/LI/HI 五種反應等級 |

**`modules/gaze_estimation/` 視線估計子模組：**

| 檔案 | 功能 |
|------|------|
| `gaze_pipeline.py` | 五階段視線估計管線整合入口 |
| `stage1_face_detection.py` | YOLO nano 人臉定位 + MediaPipe FaceMesh 468 特徵點提取 |
| `stage2_head_pose.py` | `solvePnP` 頭部姿態（Pitch/Yaw/Roll）求解 |
| `stage3_normalization.py` | 依頭部姿態正規化眼部影像至標準視角 |
| `stage4_gaze_network.py` | ETH-XGaze ResNet-50 視線推理，輸出 (pitch, yaw) |
| `stage5_gaze_vector.py` | 角度轉 3D 視線向量並投影至 2D 畫面座標 |
| `camera_utils.py` | 相機內參工具 |
| `config_loader.py` | 視線模組參數設定載入（如 pitch 偏移校正值） |
| `state_manager.py` | 時序 FSM，處理極端轉頭時的盲追蹤 |
| `visualization.py` | 視線箭頭與人臉框視覺化繪製 |

### 3.4 為何採用「推論與繪圖分離架構」

系統同時運行 4 種以上 AI 模型（OCR、物件偵測 YOLO、骨架偵測 YOLO-Pose、MediaPipe、視線估計管線），若所有模型共用同一份影像幀，先執行的模型所繪製的框線與文字會污染後續模型的輸入，例如：OCR 誤將 YOLO 標註的英文字樣判讀為牌子數字；YOLO 框線壓住機器人邊緣導致漏偵測；MediaPipe 偵測臉部特徵點時若疊加了視線箭頭繪製，會造成特徵點偏移、視線箭頭抖動。

**解決方式：** 每幀開頭複製一份乾淨影像（`display_frame = frame.copy()`），所有模型推論皆讀取原始 `frame`，僅繪圖階段統一畫在 `display_frame` 上，最終輸出與預覽皆使用 `display_frame`。

### 3.5 三種 Stage 切換機制（優先順序）

1. **視覺判斷（OCR，Stage 1-7）**：以數字牌辨識搭配投票機制確認升階。
2. **絕對時間軸推進（Stage 8-14）**：初始化時由語音快取推算各 Stage 起始秒數，主迴圈逐幀比對時間到點即強制推進。
3. **聽覺代償（Stage 7→8）**：若 Stage 7 牌子未被 OCR 偵測到，當偵測到 `noise.wav` 命中窗口時，強制跳轉至 Stage 8，作為視覺判斷失效時的備援機制。

---

## 4. 組員分工

| 學號 | 姓名 | 主要貢獻 |
|------|------|----------|
| B1109240 | 劉立綸 | 整體視線估計模組、人工標記資料集建立 |
| B1229059 | 黃璿軒 | 指向互動分析模組、牌子辨識、多模態整合、施測流程標準化 |
| B1229011 | 李瑋恆 | 訓練相關 YOLO 模型、多模態整合、引入評分系統、施測流程標準化 |
| B1229056 | 黃昱翔 | 協助修改語音辨識模組 |

---

## 參考文獻（核心模型引用）

- **ETH-XGaze**：Zhang, X., Park, S., Beeler, T., Bradley, D., Tang, S., & Hilliges, O. (2020). *ETH-XGaze: A Large Scale Dataset for Gaze Estimation under Extreme Head Poses and Gaze Directions*. ECCV 2020.
- **YOLOv8_head_detector**：Abcfsa (2023). Head Detection Model based on YOLOv8. GitHub Repository.
- **Ultralytics YOLO**（YOLOv11-Pose 及各客製化偵測模型基底架構）
- **MediaPipe**（Face Landmarker / Hand Landmarker，Google）
- **OpenAI Whisper large-v3**（語音辨識）
- **EasyOCR**（牌子數字辨識）

> 本系統不取代醫療診斷，僅供輔助研究分析使用，臨床應用仍須諮詢專業醫療人員。

---

*本文件依專案 README 與系統架構說明彙整，供論文寫作參考；如需調整段落詳略程度或補充特定模組的量化效能數據（如各模型準確率、FPS 等），請提供相關實驗數據以便補充。*
